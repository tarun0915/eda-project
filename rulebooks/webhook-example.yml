- name: Test webhook activation
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000
  rules:
    - name: Always fire on any event
      condition: true
      action:
        debug:
          msg: "Received event: {{ event }}"
